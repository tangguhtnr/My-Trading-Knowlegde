# Security

Do not commit:

- API keys;
- broker credentials;
- MT5 account numbers/passwords;
- private trade statements;
- `.env` files;
- raw proprietary datasets unless explicitly intended for publication.

If a secret is committed, rotate it immediately and rewrite history before publishing.
